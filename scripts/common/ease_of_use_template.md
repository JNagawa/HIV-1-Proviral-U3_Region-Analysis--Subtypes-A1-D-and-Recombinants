# Ease-of-use notes: <STAGE NAME>

Fill this in by hand after running `compare_<stage>.sh`. This is qualitative
information the automated metrics in `summary.tsv` can't capture, and it's
meant to feed directly into the thesis methodology section's tool
justification.

For each tool compared in this stage:

## <tool name>

- **Setup friction** (1-5, 1=trivial): 
- **Documentation quality**: 
- **Failure modes encountered** (crashes, cryptic errors, silent bad output):
- **Output usability** (easy to parse? standard format? needs custom scripting?):
- **Would you use this again for the full 24/9-sample cohort? Why/why not**:

## Overall recommendation for this stage

Which tool wins, and why (cite both `summary.tsv` numbers and the notes
above) -- this paragraph is what goes into the methods section.
